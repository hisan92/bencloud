{
  "name": "bencloud",
  "version": "0.0.1-dev",
  "private": true,
  "scripts": {
    "dev": "bun run --inspect --hot src/main.ts",
    "build": "bun build ./src/main.ts --target bun --outdir ./dist"
  },
  "dependencies": {
    "hono": "^4.5.11",
    "ofetch": "^1.3.4",
    "redis": "^4.7.0",
    "znv": "^0.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vencord/types": "^1.8.4"
  }
}
